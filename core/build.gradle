dependencies {
    implementation project(':common')

    compileOnly 'jakarta.persistence:jakarta.persistence-api'   // @Entity, @Id 등
    compileOnly 'org.springframework.data:spring-data-jpa'       // AuditingEntityListener
}

tasks.named('bootJar') {
    enabled = false
}
